{
  "name": "@zode/web-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Real-process browser E2E infrastructure for Zode",
  "scripts": {
    "smoke": "playwright test --config=playwright.config.cjs --project=chromium --grep @harness-smoke",
    "test": "playwright test --config=playwright.config.cjs"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  },
  "packageManager": "pnpm@11.20.0"
}
